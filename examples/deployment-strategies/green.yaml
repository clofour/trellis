name: shop-green
namespace: default
task_groups:
  - name: web
    count: 3
    labels: {route: shop-green, color: green}
    tasks:
      - name: web
        image: docker.io/library/nginx:1.27-alpine
        networking:
          mode: host
          ports: [{port: 80}]
        health_check: {type: http, port: 80, path: /}
